<div class="form-check">
    <input id="bun{{bun_id}}" class="form-check-input" type="radio" name="bun" value="{{bun_id}}">
    <label class="form-check-label" for="bun{{bun_id}}">
        {{bun}}
    </label>
</div>